# User management location

User lifecycle, roles, permissions and account status are implemented in
[`AccountSecurity`](../AccountSecurity/README.md). Add related commands, queries,
validators and contracts there; this legacy placeholder is not a separate module.
